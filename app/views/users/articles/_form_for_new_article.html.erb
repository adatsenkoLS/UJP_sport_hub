<%= form_for @article do |f|%>

<div>
    <div>
        <%= f.select(:conference, ['1', '2', '3'], {}, {:class => 'my_style'})  %>
    </div>
    <div>
      
        <%= f.collection_select(:teams_id, Team.all, :id, :team_name,{},{class:'dropdown-menu'}) %>
        <% end %>
    </div>
</div>